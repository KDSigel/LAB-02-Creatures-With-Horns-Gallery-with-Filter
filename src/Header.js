import React, { Component } from 'react'

export default class Header extends Component {

// need to run thorough all the data and the create 
// option tags for each one who's value is {this.props.title}

    render() {
        return (
            <div>
                <select>
                    {/* enter all those option tags here */}
                </select>
            </div>
        )
    }
}
